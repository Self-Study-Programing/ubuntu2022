#include<stdio.h>
#define MAXLINE 100

// 문자열을 입력 받아 긴 문자열일 때 출력 ...하는 프로그램
void copy(char from[], char to[]);
char line[MAXLINE];
char instr[MAXLINE];

int main(){
	// 정상종료 일때는 0을 리턴한다.
	return 0;
}
void copy(char from[], char to[]){

